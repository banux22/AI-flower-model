version: '3.8'

services:
  flower-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./static:/app/static
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped